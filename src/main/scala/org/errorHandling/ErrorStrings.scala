package org.errorHandling

object ErrorStrings {
  
  def fromErrorComponent = "show error string in ErrorComponent"
  
  def minMaxValue = "minValue is smaller or maxValue is greater as definition in configSttings"
  
  def existigOfMoreStep = "exist more as one step"
  
  def valueForImmutableComponent = "ImmutableComponent allowed not parameter for value"
  
  def selectionFewComponents = "selected to few components"
  
  def selectionMatchComponents = "selected to match components"
  
  def notFoundSteps = "The selected components has not been found in any configuration steps"
  
  def notFoundNextStep = "nextSteps for selectedComponentIds was not same"
  
  
}

